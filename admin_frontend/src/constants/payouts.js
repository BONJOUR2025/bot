/** Типы выплат — один список на обе формы панели.
 *
 *  Держится в синхроне с app/core/constants.py::PAYOUT_TYPES. Раньше список
 *  был скопирован в форму сотрудника и в админскую по отдельности, и новый тип
 *  появлялся в одной, а в другой — нет.
 *
 *  Список намеренно явный, а не из справочника `GET /dictionary/`: тот
 *  подмешивает значения из уже созданных выплат, и одна опечатка осела бы в
 *  форме навсегда. Так уже вышло с методами выплат, где рядом живут «Из кассы»
 *  и «🏦 Из кассы».
 *
 *  На расчёт зарплаты влияют только «Аванс» (вычитается) и «Зарплата»
 *  (обнуляет счёт авансов). «Премия» — доплата сверх, а не вперёд, поэтому из
 *  зарплаты не вычитается.
 */
export const PAYOUT_TYPES = ['Аванс', 'Зарплата', 'Премия'];

/** Что сотрудник ждёт к выплате: запрошенное им, но не сама зарплата.
 *  По ним считается «сколько мне должны сверх оклада». */
export const REQUESTABLE_TYPES = ['Аванс', 'Премия'];
